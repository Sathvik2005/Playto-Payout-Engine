# Local dev can stay empty because Vite proxies `/api` to the Django server.
# For Vercel, set this to the deployed backend origin, for example https://playto-payout-engine.onrender.com
VITE_API_BASE_URL=
