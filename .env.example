# Frontend runtime environment for Vercel.
# Local development can stay empty because Vite proxies `/api` to the Django server.
# For production, point this to the Render backend URL.
VITE_API_BASE_URL=https://playto-payout-engine.onrender.com
